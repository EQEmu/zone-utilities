#pragma once

#include <string>

namespace eqemu {
    namespace string {
        std::string to_lower(const std::string& str);
        std::string to_upper(const std::string& str);
    }    // namespace string
}    // namespace eqemu
